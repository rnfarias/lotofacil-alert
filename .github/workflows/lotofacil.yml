“`yaml
name: Aviso Lotofacil

on:
  schedule:
    - cron: '0 20 * * *'
  workflow_dispatch:

jobs:
  run-script:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-python@v4
        with:
          python-version: '3.11'
      - run: pip install requests
      - run: python lotofacil_alert.py
        env:
          TOKEN:{{ secrets.TOKEN }}
          CHAT_ID: ${{ secrets.CHAT_ID }}
